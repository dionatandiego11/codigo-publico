# Shared

Componentes e utilidades compartilhados entre features.

Use `shared/` apenas para peças realmente reutilizáveis ou de estrutura global, como layout, providers e componentes base. Código específico de um domínio deve continuar dentro da respectiva feature.
