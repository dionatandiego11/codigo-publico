# Configuração de build de PRODUÇÃO (lida automaticamente pelo Vite).
# A API é servida no mesmo domínio, atrás do nginx (/api/v1 -> :8080),
# então a URL é RELATIVA. Nunca aponte para localhost aqui.
VITE_API_URL=/api/v1
